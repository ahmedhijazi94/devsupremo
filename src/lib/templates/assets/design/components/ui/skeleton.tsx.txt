import { cn } from '@/lib/utils'
export function Skeleton({ className, ...props }: React.ComponentProps<'div'>) {
  return <div aria-hidden="true" className={cn('bg-elevated motion-safe:animate-pulse rounded-[var(--radius-md)]', className)} {...props} />
}
