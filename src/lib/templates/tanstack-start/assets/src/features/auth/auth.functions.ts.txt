import { createServerFn } from '@tanstack/react-start'
import { z } from 'zod'
import { currentUser } from './auth.server'

export const getCurrentUser = createServerFn({ method: 'GET' }).validator(z.undefined()).handler(async () => currentUser())
